#? Config file for btop

#* Name of a btop++/bpytop/bashtop formatted ".theme" file, "Default" and "TTY" for builtin themes.
color_theme = "noctalia"

#* If the theme set background should be shown, set to False if you want terminal background transparency.
theme_background = True

#* Sets if 24-bit truecolor should be used, will convert 24-bit colors to 256 color (6x6x6 color cube) if false.
truecolor = True

#* Set to true to force tty mode regardless if a real tty has been detected or not.
force_tty = False

#* Set how many boxes should be shown: only CPU and Processes (tasks).
shown_boxes = "cpu proc"

#* Update time in milliseconds, recommended 2000 ms or above for better sample times for graphs.
update_ms = 2000

#* Processes sorting, "pid" "program" "arguments" "threads" "user" "memory" "cpu lazy" "cpu direct".
proc_sorting = "cpu lazy"

#* Reverse sorting order, True or False.
proc_reversed = False

#* Show processes as a tree.
proc_tree = True

#* Use the cpu graph colors in the process list.
proc_colors = True

#* Use a darkening gradient in the process list.
proc_gradient = True

#* If process cpu usage should be of the core it's running on or usage of the total available cpu power.
proc_per_core = False

#* Show process memory as bytes instead of percent.
proc_mem_bytes = True

#* Show cpu graph for each process.
proc_cpu_graphs = True

#* Enable information about selected process over the process list.
proc_info_smaps = False

#* Show detailed information for a selected process on left side of the process box.
proc_left = False

#* Sets the CPU stat shown in upper half of the CPU graph.
cpu_graph_upper = "Auto"

#* Sets the CPU stat shown in lower half of the CPU graph.
cpu_graph_lower = "Auto"

#* Toggles if the lower CPU graph should be inverted.
cpu_invert_lower = True

#* Set to True to completely disable the lower CPU graph.
cpu_single_graph = False

#* Show cpu box at bottom of screen instead of top.
cpu_bottom = False

#* Shows the system uptime in the CPU box.
show_uptime = True

#* Show cpu temperature.
check_temp = True

#* Which sensor to use for cpu temperature.
cpu_sensor = "Auto"

#* Show temperatures for cpu cores also if check_temp is True.
show_coretemp = True

#* Set to true to enable "h,j,k,l,g,G" keys for directional control in lists.
vim_keys = False

#* Rounded corners on boxes.
rounded_corners = True

#* Default symbols to use for graph creation.
graph_symbol = "braille"
